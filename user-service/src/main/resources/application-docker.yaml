spring:
  datasource:
    url: jdbc:postgresql://user-db:5432/user_db
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
server:
  port: 8081