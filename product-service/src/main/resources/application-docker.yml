spring:
  datasource:
    url: jdbc:postgresql://product-db:5432/product_db
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

server:
  port: 8082